<%= form_with(model: car) do |form| %>
  <% if car.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(car.errors.count, "error") %> prohibited this car from being saved:</h2>

      <ul>
        <% car.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :modelo %>
    <%= form.text_field :model %>
  </div>

  <div class="field">
    <%= form.label :año %>
    <%= form.number_field :year %>
  </div>

  <div class="field">
    <%= form.label :color %>
    <%= form.text_field :color %>
  </div>

  <div class="field">
    <%= form.label :patente %>
    <%= form.text_field :car_plate %>
  </div>

  <div class="field">
    <%= form.label :marca %>
    <%= form.collection_select(:brand_id, Brand.all, :id, :name) %>
    <%# el colletion_select es para q te despliegue las opciones de marcas. %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
