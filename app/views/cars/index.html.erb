<p id="notice"><%= notice %></p>

<h1>Cars</h1>

<form method="GET" action="/cars">
  Patente: <input type="text" name="filter" />
  <input type="submit">
</form><br>

<table>
  <thead>
    <tr>
      <th>Modelo</th>
      <th>Año</th>
      <th>Color</th>
      <th>Patente</th>
      <th>Marca</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @cars.each do |car| %>
      <tr>
        <td><%= car.model %></td>
        <td><%= car.year %></td>
        <td><%= car.color %></td>
        <td><%= car.car_plate unless car.car_plate.nil? %></td>
        <td><%= car.brand.name unless car.brand.nil? %></td>
        <td><%= link_to 'Show', car %></td>
        <td><%= link_to 'Edit', edit_car_path(car) %></td>
        <td><%= link_to 'Destroy', car, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Car', new_car_path %>
